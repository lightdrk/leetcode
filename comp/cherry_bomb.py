from collections import Counter

def func(a: list[int],b: list[int]):
    if len(Counter(b)) == 1:
        return 1
    l = len(a)
    mn = min(a)
    mx = max(a)

    for i in range(mn,mx+1):
        if 
    
